@using DeKayaServer.BlazorApp.Components.Common.Dialog
@using DeKayaServer.BlazorApp.Models
@inject IUserService userService
@inject IApiResultPresenter apiResultPresenter

<Dialog
    @ref="dialog"
    Title="Kullanıcıyı sil">
    <ChildContent>
        <div class="py-3">
            <p>Kullanıcı <span class="fw-bolder">@FullName</span> silmek istediğinize emin misiniz?</p>
        </div>
        <div class="d-flex gap-3 justify-content-end mt-4 pt-3 border-top">
            <button type="button"
                    class="btn btn-outline-secondary px-4 py-2 fw-semibold"
                    @onclick="HideAsync"
                    disabled="@isSubmit">
                İptal
            </button>
            <button type="button"
                    class="btn btn-danger px-4 py-2 fw-semibold shadow-sm"
                    @onclick="HandleDeleteAsync"
                    disabled="@isSubmit">
                Sil
            </button>
        </div>
    </ChildContent>
</Dialog>

@code {
    private Dialog? dialog;
    private bool isSubmit;

    [Parameter] public Guid? UserId { get; set; }
    [Parameter] public string? FullName { get; set; }
    [Parameter] public EventCallback UserDeleted { get; set; }

    public async Task ShowAsync()
    {
        if ( dialog is not null )
            await dialog.ShowAsync();
    }

    public async Task HideAsync()
    {
        if ( dialog is not null )
            await dialog.HideAsync();
    }

    private async Task HandleDeleteAsync()
    {
        if(UserId is null)
            return;

        try
        {
            isSubmit = true;
            var result = await userService.DeleteAsync( UserId.Value );

            apiResultPresenter.Present( result, new ApiPresentationOptions { ShowSuccess = true } );

            if(!result.IsSuccessful)
                return;

            if(UserDeleted.HasDelegate)
                await UserDeleted.InvokeAsync();
        }
        finally
        {
            isSubmit = false;
            await HideAsync();
        }
    }
}
