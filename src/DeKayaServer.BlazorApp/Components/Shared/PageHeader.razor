@inject NavigationManager NavigationManager

<PageTitle>De-Kaya Systems | @Title</PageTitle>

<div class="page-header">
    <div class="page-title">
        <h1>
            @if (!string.IsNullOrWhiteSpace(Icon))
            {
                <i class="bi @Icon text-primary me-2"></i>
            }
            @Title
        </h1>
        @if (!string.IsNullOrWhiteSpace(Description))
        {
            <p class="page-subtitle">@Description</p>
        }
    </div>

    <div class="page-actions">
        @if (ShowStatus)
        {
            <div class="status-badge">
                @if (Status)
                {
                    <span class="badge bg-success">Active</span>
                }
                else
                {
                    <span class="badge bg-danger">Inactive</span>
                }
            </div>
        }

        @if (ShowBackButton)
        {
            <button type="button" class="btn btn-outline-secondary" @onclick="BackButtonClicked">
                <i class="bi bi-arrow-left me-1"></i>Back
            </button>
        }

        @if (ShowEditButton && !string.IsNullOrWhiteSpace(EditBtnUrl))
        {
            <div class="btn-group">
                <a href="@EditBtnUrl" class="btn btn-primary">
                    <i class="bi bi-pencil me-1"></i>Edit
                </a>
            </div>
        }

        @Actions
    </div>
</div>

@code {
    [Parameter, EditorRequired]
    public string? Title { get; set; } = default!;

    [Parameter]
    public string? Icon { get; set; }

    [Parameter]
    public string? Description { get; set; }

    [Parameter]
    public bool ShowStatus { get; set; }

    [Parameter]
    public bool Status { get; set; } = true;

    [Parameter]
    public bool ShowBackButton { get; set; } = true;

    [Parameter]
    public bool ShowEditButton { get; set; }

    [Parameter]
    public string? EditBtnUrl { get; set; }

    [Parameter]
    public RenderFragment? Actions { get; set; }

    private void BackButtonClicked() => NavigationManager.NavigateTo("javascript:history.back()");
}
